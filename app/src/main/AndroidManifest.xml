<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CookIm"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">


        <activity
            android:name=".controller.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="android.app.lib_name"
            android:value="" />

        <activity
            android:name=".controller.PresentationActivity"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:exported="true" />

        <!--The singleTask hace que un activity solo pueda existir una vez-->
        <activity
            android:name=".controller.LoginActivity"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:launchMode="singleTask"
            android:exported="true"
            />

        <activity
            android:name=".controller.Home.HomeActivity"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:exported="true">



        </activity>

        <activity
            android:name=".controller.RecipePage"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:exported="true"/>

        <activity
            android:name=".controller.SignActivity"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:exported="true"/>

        <activity
            android:name=".controller.RecipeStepsActivity"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:exported="true" />

        <activity
            android:name=".controller.MyProfileActivity"
            android:theme="@style/Theme.CookIm.NoActionBar"
            android:exported="true" />

    </application>

</manifest>